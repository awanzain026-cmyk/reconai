# ReconAI backend — copy to .env and fill in. NEVER commit .env.
# Local default (no .env needed): SQLite file in backend/.

# Postgres connection string. For Neon (Stage 7) use:
#   DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST/dbname?sslmode=require
DATABASE_URL=sqlite:///./recon.db
[project]
name = "reconai-backend"
version = "0.1.0"
description = "ReconAI reconciliation API backend"
requires-python = "==3.12.*"

[tool.fastapi]
entrypoint = "main:app"

[tool.setuptools]
py-modules = ["auth", "db", "importer", "main", "matcher", "models"]
